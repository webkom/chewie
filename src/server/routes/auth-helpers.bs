import ../config
export {
  isAuthenticated: isAuthenticated
}

isAuthenticated = (req, res, next) ->
  if not config.PASSPORT_STRATEGY or req.isAuthenticated()
    next()
  else
    res.redirect('/auth/login')
