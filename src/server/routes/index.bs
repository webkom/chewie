import express
import request

import ../config
import ./auth-helpers as authHelpers: isAuthenticated

export router

router = express.Router()

router.get('/', authHelpers.isAuthenticated, (req, res) ->
  res.render('index', { projects: require(config.SERVER_CONFIG_FILE) })
)
